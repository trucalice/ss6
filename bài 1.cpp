#include <stdio.h>

int main() {
    int a, b, c, d, e,chan,le;      
    printf("Nhap 5 so nguyen bat ky: \n");
    scanf("%d%d%d%d%d", &a, &b, &c, &d, &e);
    if (a % 2 == 0) {
        chan = a;}
    else {
        le = a;
    }
    if (b % 2 == 0) {
        chan = b;}
    else {
        le = b;
    }
    if (c % 2 == 0) {
        chan = c;}
    else {
        le = c;
    }
    if (d % 2 == 0) {
        chan = d;}
    else {
        le = d;
    }
    if (e % 2 == 0) {
        chan = e;}
    else {
        le = e;
    }
    return 0;
}

